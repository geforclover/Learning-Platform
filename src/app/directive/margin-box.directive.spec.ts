import { MarginBoxDirective } from './margin-box.directive';

describe('MarginBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new MarginBoxDirective();
    expect(directive).toBeTruthy();
  });
});
