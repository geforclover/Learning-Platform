import { TableViewCloseDirective } from './table-view-close.directive';

describe('TableViewCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new TableViewCloseDirective();
    expect(directive).toBeTruthy();
  });
});
