import { ColWidthDirective } from './col-width.directive';

describe('ColWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ColWidthDirective();
    expect(directive).toBeTruthy();
  });
});
