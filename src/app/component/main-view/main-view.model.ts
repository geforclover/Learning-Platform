export class MainModel {
    icon: string;
    name: string;
    url: string;

    constructor(icon: string, name: string, url: string) {
        this.icon = icon;
        this.name = name;
        this.url = url;
    }
}
